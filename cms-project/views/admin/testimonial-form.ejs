<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - MCA CMS</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-gray-50">
    
    <!-- Include Sidebar -->
    <%- include('partials/sidebar') %>

    <!-- Main Content -->
    <div class="lg:ml-64">
        <!-- Header -->
        <header class="bg-white shadow-sm border-b border-gray-200">
            <div class="flex items-center justify-between px-6 py-4">
                <div class="flex items-center">
                    <button class="lg:hidden text-gray-600 hover:text-gray-900 mr-4" id="sidebar-toggle">
                        <i class="fas fa-bars text-xl"></i>
                    </button>
                    <h1 class="text-2xl font-bold text-gray-900"><%= title %></h1>
                </div>
                
                <!-- User Menu -->
                <div class="flex items-center space-x-4">
                    <a href="/admin/testimonials" class="bg-gray-600 text-white px-4 py-2 rounded-md hover:bg-gray-700">
                        <i class="fas fa-arrow-left mr-2"></i>Back to Testimonials
                    </a>
                    <div class="relative">
                        <button class="flex items-center text-gray-700 hover:text-gray-900" id="user-menu-button">
                            <i class="fas fa-user-circle text-2xl mr-2"></i>
                            <span>Admin</span>
                            <i class="fas fa-chevron-down ml-2"></i>
                        </button>
                    </div>
                </div>
            </div>
        </header>

        <!-- Content -->
        <main class="p-6">
            <div class="max-w-4xl mx-auto">
                <div class="bg-white rounded-lg shadow">
                    <div class="px-6 py-4 border-b border-gray-200">
                        <h2 class="text-lg font-semibold text-gray-900"><%= title %></h2>
                    </div>
                    
                    <form action="<%= action %>" method="POST" class="p-6">
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                            <!-- Customer Name -->
                            <div>
                                <label for="name" class="block text-sm font-medium text-gray-700 mb-2">Customer Name *</label>
                                <input type="text" 
                                       id="name" 
                                       name="name" 
                                       value="<%= testimonial ? testimonial.name : '' %>"
                                       required 
                                       class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
                            </div>

                            <!-- Customer Title/Position -->
                            <div>
                                <label for="title" class="block text-sm font-medium text-gray-700 mb-2">Title/Position *</label>
                                <input type="text" 
                                       id="title" 
                                       name="title" 
                                       value="<%= testimonial ? testimonial.title : '' %>"
                                       required 
                                       placeholder="e.g., CEO, Customer, Client"
                                       class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
                            </div>

                            <!-- Customer Image -->
                            <div>
                                <label for="image" class="block text-sm font-medium text-gray-700 mb-2">Customer Image URL</label>
                                <input type="url" 
                                       id="image" 
                                       name="image" 
                                       value="<%= testimonial ? testimonial.image : '' %>"
                                       placeholder="https://example.com/customer-photo.jpg"
                                       class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
                                <p class="text-xs text-gray-500 mt-1">Enter a URL to the customer's photo or upload it to /media/ folder</p>
                            </div>

                            <!-- Rating -->
                            <div>
                                <label for="rating" class="block text-sm font-medium text-gray-700 mb-2">Rating *</label>
                                <select id="rating" 
                                        name="rating" 
                                        required 
                                        class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
                                    <% for (let i = 1; i <= 5; i++) { %>
                                        <option value="<%= i %>" <%= (testimonial && testimonial.rating === i) ? 'selected' : '' %>>
                                            <%= i %> Star<%= i !== 1 ? 's' : '' %>
                                        </option>
                                    <% } %>
                                </select>
                            </div>

                            <!-- Video URL -->
                            <div>
                                <label for="video" class="block text-sm font-medium text-gray-700 mb-2">Video URL (Optional)</label>
                                <input type="url" 
                                       id="video" 
                                       name="video" 
                                       value="<%= testimonial ? testimonial.video : '' %>"
                                       placeholder="https://example.com/testimonial-video.mp4"
                                       class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
                                <p class="text-xs text-gray-500 mt-1">Optional video testimonial URL</p>
                            </div>

                            <!-- Video Thumbnail -->
                            <div>
                                <label for="videoThumbnail" class="block text-sm font-medium text-gray-700 mb-2">Video Thumbnail URL (Optional)</label>
                                <input type="url" 
                                       id="videoThumbnail" 
                                       name="videoThumbnail" 
                                       value="<%= testimonial ? testimonial.videoThumbnail : '' %>"
                                       placeholder="https://example.com/video-thumbnail.jpg"
                                       class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
                                <p class="text-xs text-gray-500 mt-1">Thumbnail image for the video</p>
                            </div>
                        </div>

                        <!-- Testimonial Content -->
                        <div class="mt-6">
                            <label for="content" class="block text-sm font-medium text-gray-700 mb-2">Testimonial Content *</label>
                            <textarea id="content" 
                                      name="content" 
                                      rows="5" 
                                      required 
                                      placeholder="Enter the customer's testimonial..."
                                      class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent"><%= testimonial ? testimonial.content : '' %></textarea>
                        </div>

                        <!-- Status Options -->
                        <div class="mt-6">
                            <h3 class="text-sm font-medium text-gray-700 mb-3">Status Options</h3>
                            <div class="space-y-3">
                                <div class="flex items-center">
                                    <input type="checkbox" 
                                           id="published" 
                                           name="published" 
                                           <%= (!testimonial || testimonial.published) ? 'checked' : '' %>
                                           class="h-4 w-4 text-indigo-600 focus:ring-indigo-500 border-gray-300 rounded">
                                    <label for="published" class="ml-2 block text-sm text-gray-900">
                                        Published
                                        <span class="text-gray-500">(visible on website)</span>
                                    </label>
                                </div>
                                
                                <div class="flex items-center">
                                    <input type="checkbox" 
                                           id="featured" 
                                           name="featured" 
                                           <%= (testimonial && testimonial.featured) ? 'checked' : '' %>
                                           class="h-4 w-4 text-indigo-600 focus:ring-indigo-500 border-gray-300 rounded">
                                    <label for="featured" class="ml-2 block text-sm text-gray-900">
                                        Featured
                                        <span class="text-gray-500">(appears in hero section)</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <!-- Preview Section -->
                        <% if (testimonial && testimonial.image) { %>
                            <div class="mt-6">
                                <h3 class="text-sm font-medium text-gray-700 mb-3">Current Image Preview</h3>
                                <img src="<%= testimonial.image %>" alt="<%= testimonial.name %>" class="h-20 w-20 rounded-full object-cover">
                            </div>
                        <% } %>

                        <!-- Submit Buttons -->
                        <div class="mt-8 flex justify-end space-x-4">
                            <a href="/admin/testimonials" class="px-4 py-2 border border-gray-300 rounded-md text-gray-700 hover:bg-gray-50">
                                Cancel
                            </a>
                            <button type="submit" class="px-6 py-2 bg-indigo-600 text-white rounded-md hover:bg-indigo-700">
                                <%= testimonial ? 'Update Testimonial' : 'Create Testimonial' %>
                            </button>
                        </div>
                    </form>
                </div>

                <!-- Help Section -->
                <div class="mt-6 bg-blue-50 border border-blue-200 rounded-lg p-4">
                    <h3 class="text-sm font-medium text-blue-800 mb-2">Tips for Great Testimonials</h3>
                    <ul class="text-sm text-blue-700 space-y-1">
                        <li>• Keep testimonials authentic and specific</li>
                        <li>• Include the customer's full name and position for credibility</li>
                        <li>• Use high-quality, professional photos</li>
                        <li>• Featured testimonials appear in the hero section (limit to 2-3)</li>
                        <li>• Published testimonials appear in the main testimonials grid</li>
                    </ul>
                </div>
            </div>
        </main>
    </div>

    <!-- Include Sidebar JavaScript -->
    <%- include('partials/sidebar-script') %>
</body>
</html>
